<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.StylishAdmin" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Primary Colors -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorSecondary">@color/secondrey_text_color_dark</item>
        <item name="colorItemBackground">@color/field_color_dark</item>
        <item name="colorBrandBackground">@color/secondrey_text_color_dark</item>

        <!-- Background Colors -->
        <item name="navigationBackground">@color/background_color_dark</item>

        <item name="android:colorBackground">@color/background_color_dark</item>
        <item name="colorBackgroundFloating">@color/field_color_dark</item>

        <!-- Text Colors -->
        <item name="colorOnBackground">@color/text_color_dark</item>
        <item name="colorOnSurface">@color/secondrey_text_color_dark</item>

        <!-- Misc Colors -->
        <item name="colorOutline">@color/field_color_dark</item>
    </style>
</resources>
